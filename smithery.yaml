# Smithery configuration file for Change Firewall MCP Server
# https://smithery.ai/docs/config
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    - npx
    - -y
    - change-firewall
    - mcp
